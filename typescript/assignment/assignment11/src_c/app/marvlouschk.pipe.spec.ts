import { MarvlouschkPipe } from './marvlouschk.pipe';

describe('MarvlouschkPipe', () => {
  it('create an instance', () => {
    const pipe = new MarvlouschkPipe();
    expect(pipe).toBeTruthy();
  });
});
